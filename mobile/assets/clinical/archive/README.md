# Archived clinical assets

`tooth-preview-grid.legacy.png` — original Elite Dent 5×4 smile photo mosaic
(20 cells). Removed from active Smile Preview UI in favor of individual
`assets/clinical/shapes/shape_XX_*.png` images.

Restore path (also in code as `ShapeLibrary.legacyGridAsset`):

```
assets/clinical/archive/tooth-preview-grid.legacy.png
```

Grid layout if you crop it again: **5 columns × 4 rows** (row-major).
