# Vendored ML repos

## individual_tooth_segmentation (KAIST)

```bash
cd backend
git clone --depth 1 https://github.com/mireiffe/individual_tooth_segmentation.git \
  vendor/individual_tooth_segmentation
```

Or: `python scripts/run_kaist_pilot.py --setup`

This directory is gitignored — clone locally, do not commit the upstream tree.
